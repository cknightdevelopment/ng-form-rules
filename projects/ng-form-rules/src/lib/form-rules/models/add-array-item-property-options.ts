/**
 * Options when adding an array item property to an existing form
 */
export interface AddArrayItemPropertyOptions {
    /**
     * Index of where to insert the new item into the existing FormArray
     */
    index?: number;
}