import { PropertyBase } from "./property-base";

/**
 * A property representing an array item type (e.g. an array of Address objects)
 */
export class ArrayItemProperty<T> extends PropertyBase<T> {}